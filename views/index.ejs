<%- include('./partials/header') %>

<div class="container">
    <div class="row">
    <div class="offset-md-3 col-md-6 ps-0 pe-0">

        <div>
            <h2>Recalling details from conversations can be hard.</h2>
            <h2>Let's fix that.</h2>
        </div>

        <div>
            <% if (user) { %>
            <a href="/logout" class="btn btn-outline-primary btn-lg" role="button">Log out</a>
            <% } else { %>
            <a href="/auth/google" class="btn btn-lg btn-outline-primary" role="button">Log in with Google</a>
            <% } %>
        </div>

    </div>        
    </div>        
</div>        

<%- include('./partials/footer') %>